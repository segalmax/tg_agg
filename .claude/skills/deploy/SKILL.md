---
name: deploy
description: Deploy tg_agg to production. Use when the user runs /deploy, asks to deploy, ship, release, or push to production.
tools: Bash, Read, WebFetch, mcp__Claude_in_Chrome__navigate, mcp__Claude_in_Chrome__computer, mcp__Claude_Preview__preview_screenshot
disable-model-invocation: true
---

Read the deploy workflow from the single source of truth and execute it:

```bash
cat .cursor/skills/deploy/SKILL.md
```

Then follow every step in that file exactly.
